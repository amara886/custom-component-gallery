/**
 * EditorJS Component Module
 * Main barrel export for the Editor.js Retool custom component
 */

export { EditorComponent } from './components/EditorComponent'

// Export types for potential external use
export type {
  ToolConfig,
  EditorTools,
  ToolEnableStates,
  EditorBlock,
  EditorData,
  EditorStyling
} from './config/types'
